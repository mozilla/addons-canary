{
  "name": "canary",
  "version": "1.0.0",
  "description": "Canary extension for testing blocks",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "MPL-2.0",
  "dependencies": {
    "web-ext": "5.5.0"
  },
  "scripts": {
    "build": "web-ext build -s src/ -a build/ --overwrite-dest",
    "try": "web-ext run -s src/"
  }
}
